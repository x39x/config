[user] 
name = Y.S.
email = x39x@gmail.com 
[core]
excludesfile = ~/.config/git/gitignore
editor = nvim
pager = delta

[merge]
tool = nvimdiff
conflictstyle = diff3
[mergetool]
keepBackup = false 
[mergetool "nvimdiff"]
cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[diff]
colorMoved = default 
[interactive]
diffFilter = delta --color-only
[delta]
side-by-side = true

#WARN: PASSWORD
[credential]
helper = store
